import type { GameDefinition } from '../engine';
import { makeTrial } from './shared';

export interface MazeTrial { trialId: string; sessionId: string; gameId: string; gameVersion: string; trialIndex: number; domain: 'attention-visual-search'; difficulty: number; startedAt: string; stimulus: { size: number; grid: string[][]; targets: string[]; paths: Record<string, string[]> }; expectedResponse: string[]; score: number; attemptCount: number; errorCount: number; metadata: Record<string, unknown>; correct?: boolean; completedAt?: string; responseAt?: string; reactionTimeMs?: number; actualResponse?: unknown; }
const wordSets = [['cat', 'sun'], ['dog', 'map', 'fish'], ['moon', 'star', 'tree'], ['plant', 'cloud', 'train'], ['bright', 'friend', 'garden']];
function buildMaze(difficulty: number, random: { pick: <T>(items: T[]) => T; int: (min: number, max: number) => number }): { size: number; grid: string[][]; targets: string[]; paths: Record<string, string[]> } { const size = difficulty > 2 ? 8 : 6; const targets = wordSets[Math.min(4, difficulty - 1)]; const grid = Array.from({ length: size }, () => Array.from({ length: size }, () => String.fromCharCode(65 + random.int(0, 25)))); const paths: Record<string, string[]> = {}; targets.forEach((word, index) => { const row = Math.min(index * 2, size - 1); const start = 0; const path = [...word].map((letter, offset) => `${row},${start + offset}`); paths[word] = path; [...word].forEach((letter, offset) => { if (start + offset < size) grid[row][start + offset] = letter.toUpperCase(); }); }); return { size, grid, targets: targets.map(word => word.toUpperCase()), paths }; }
export const wordMaze: GameDefinition<MazeTrial> = { id: 'word-maze', name: 'Word Maze', description: 'Find the words hiding along the path.', domain: 'attention-visual-search', gameVersion: '1.0.0', ageRange: [4, 10], estimatedDurationMinutes: 5, difficultyLevels: [1, 2, 3, 4, 5], instructions: ['Choose a word to find.', 'Tap letters next to each other.', 'Words can travel across, down, or diagonally.'], totalTrials: 3, createTrial: ({ index, difficulty, random, sessionId }) => { const maze = buildMaze(difficulty, random); return makeTrial(index, sessionId, 'word-maze', 'attention-visual-search', difficulty, maze, maze.targets) as MazeTrial; }, evaluateResponse: (trial, response) => { const found = Array.isArray(response) ? response : []; const target = trial.expectedResponse.find(word => found.includes(word)); return { correct: found.length === trial.expectedResponse.length, score: found.length / trial.expectedResponse.length, actualResponse: found, metadata: { foundCount: found.length, lastFound: target ?? null } }; }, getNextDifficulty: history => history.at(-1)?.difficulty ?? 1 };
