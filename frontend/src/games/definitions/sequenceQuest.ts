import type { GameDefinition } from '../engine';
import { makeTrial } from './shared';

export interface SequenceTrial { trialId: string; sessionId: string; gameId: string; gameVersion: string; trialIndex: number; domain: 'working-memory'; difficulty: number; startedAt: string; stimulus: { sequence: string[]; displayMs: number }; expectedResponse: string[]; score: number; attemptCount: number; errorCount: number; metadata: Record<string, unknown>; correct?: boolean; completedAt?: string; responseAt?: string; reactionTimeMs?: number; actualResponse?: unknown; }
const items = ['star', 'moon', 'sun', 'leaf', 'cloud', 'wave'];
export const sequenceQuest: GameDefinition<SequenceTrial> = { id: 'sequence-quest', name: 'Sequence Quest', description: 'Remember the path and rebuild it.', domain: 'working-memory', gameVersion: '1.0.0', ageRange: [4, 10], estimatedDurationMinutes: 5, difficultyLevels: [1, 2, 3, 4, 5], instructions: ['Watch the path.', 'Tap the pieces in the same order.', 'You can change your choices before you finish.'], totalTrials: 4, createTrial: ({ index, difficulty, random, sessionId }) => { const length = Math.min(2 + difficulty, 6); const sequence = random.shuffle(items).slice(0, length); return makeTrial(index, sessionId, 'sequence-quest', 'working-memory', difficulty, { sequence, displayMs: 1800 + length * 250 }, sequence) as SequenceTrial; }, evaluateResponse: (trial, response) => { const submitted = Array.isArray(response) ? response : []; const correctPositions = submitted.filter((item, index) => item === trial.expectedResponse[index]).length; return { correct: correctPositions === trial.expectedResponse.length, score: correctPositions / trial.expectedResponse.length, actualResponse: submitted, metadata: { sequenceLength: trial.expectedResponse.length, correctPositions } }; }, getNextDifficulty: history => history.at(-1)?.difficulty ?? 1 };
