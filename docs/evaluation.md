# Evaluation

Future classification evaluation should use accuracy, precision, recall, F1, ROC-AUC, PR-AUC, and confusion matrices only when the target and sample size support them. Regression targets should use MAE, MSE, RMSE, and R2. All validation must use grouped subject splits; random trial splits are prohibited.
